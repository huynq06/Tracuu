
@{
    Layout = null;
    int PageCurrent = 0;
}
<div>

</div>
<div id="content-invoice">
    <div class="row">
        <div class="col-md-12">
            <div class="col-md-6">
                <table id="tbl-msg" class="table table-striped table-bordered table-advance table-hover dt-responsive">
                    <thead>
                        <tr>
                            <th class="all" style="width:3%"></th>
                            <th style="width:10%">
                                Tên điện
                            </th>
                            <th style="width:10%">
                                Giờ nhận
                            </th>
                            @*<th style="width:10%">
                                Mô tả
                            </th>*@

                            @*<th style="width:10%;font-size:16px;background-color:yellow !important;text-align:center">
                        THAO TÁC
                    </th>*@
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var row in (IList<Web.Portal.Common.ApiViewModel.FFMViewModel>)ViewData["listFFM"])
            {
                PageCurrent += 1;
                            <tr>
                                <td>@PageCurrent</td>
                                <td><a href="#" class="btn btn-xs btn_export" data-doc="@Html.DisplayFor(model => row.URL)">FFM</a></td>
                                <td><a href="#" class="btn btn-xs btn_export" data-doc="@Html.DisplayFor(model => row.URL)">@Html.Raw(row.Created)</a></td>
                                @*<td><a href="#" class="btn btn_export  btn-xs" data-doc="@Html.DisplayFor(model => row.URL)">@Html.Raw(row.Description)</a></td>*@
                            </tr>
                        }
                        @foreach (var row in (IList<Web.Portal.Common.ApiViewModel.FSUViewModel>)ViewData["listFSU"])
            {
                PageCurrent += 1;
                            <tr>
                                <td>@PageCurrent</td>
                                <td><a href="#" class="btn btn-xs btn_export" data-doc="@Html.DisplayFor(model => row.URL)">FSU</a></td>
                                <td><a href="#" class="btn btn-xs btn_export" data-doc="@Html.DisplayFor(model => row.URL)">@Html.Raw(row.Created)</a></td>
                                @*<td><a href="#" class="btn btn-xs btn_export" data-doc="@Html.DisplayFor(model => row.URL)">@Html.Raw(row.Description)</a></td>*@
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.URL)</a></td>*@
                                @*<td>
                            <div class="btn-group">
                                <a title="Xuất báo cáo" class="btn blue btn_export" data-doc="@Html.DisplayFor(model => row.URL)">
                                    <i class="fa fa-cloud-download"></i>
                                </a>
                            </div>
                        </td>*@
                            </tr>
                        }
                        @foreach (var row in (IList<Web.Portal.Common.ApiViewModel.FHLViewModel>)ViewData["listFHL"])
            {
                PageCurrent += 1;
                            <tr>
                                <td>@PageCurrent</td>
                                <td><a href="#" class="btn edit-hawb  btn-xs">FHL</a></td>
                                <td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.Created)</a></td>
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.Description)</a></td>*@
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.URL)</a></td>*@
                            </tr>
                        }
                        @foreach (var row in (IList<Web.Portal.Common.ApiViewModel.FWBViewModel>)ViewData["listFWB"])
            {
                PageCurrent += 1;
                            <tr>
                                <td>@PageCurrent</td>
                                <td><a href="#" class="btn edit-hawb  btn-xs">FWB</a></td>
                                <td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.Created)</a></td>
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.Description)</a></td>*@
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.URL)</a></td>*@
                            </tr>
                        }
                        @foreach (var row in (IList<Web.Portal.Common.ApiViewModel.NOAViewModel>)ViewData["listNOA"])
            {
                PageCurrent += 1;
                            <tr>
                                <td>@PageCurrent</td>
                                <td><a href="#" class="btn edit-hawb  btn-xs">NOA</a></td>
                                <td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.Created)</a></td>
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.Description)</a></td>*@
                                @*<td><a href="#" class="btn edit-hawb  btn-xs">@Html.Raw(row.URL)</a></td>*@
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
            <div class="col-md-6">
                <div class="row">
                    <div class="col-md-12">
                        <div id="content-msg-result">

                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
    


</div>
<script>
    var hermesAction = {
        search: function (link) {
            egovutil.getContent("#content-msg-result", "/AwbDetail/MessageContent?link=" + link);
        },
    }
    $(".btn_export").click(function () { hermesAction.search($(this).data('doc')); })
    //$(".btn_export").click(function () {
    //    hermesAction.search
    //    // window.open("/awbDetail/OpenDoc?link=" + $(this).data('doc') + "&invoiceins=" + $(this).data('id'));
    //    //$(".btn_export").click(function () {
    //    //    egovutil.dialog("THÔNG TIN ĐIỆN VĂN", "large", "/AwbDetail/MessageContent?link=" + $(this).data('doc') + "&invoiceins=" + $(this).data('id'), "");
    //    //});
    //});
    //egovutil.CreateTableSort("#tbl-msg", [[0, "asc"]], [

    //         { "orderable": false, "targets": 1 }
    //], true);
</script>