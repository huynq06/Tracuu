@{
    Layout = null;
    int rowIndex = 0;
}
<p style="text-align:center;font-family:'Times New Roman';font-size:15pt;font-weight:bold;text-transform:uppercase">
    @ViewBag.TitleReport
</p>
<table id="tbl-hawb" class="table table-striped table-bordered table-advance table-hover" border="1">
    <thead>
        <tr>
            <th style="width:3%;text-align:center">
                STT
            </th>
            <th style="width:10%;text-align:center">
                AWB
            </th>
            <th style="width:10%;text-align:center">
                HAWB
            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var row in (IList<Web.Portal.Common.ViewModel.IrrRemarkViewModel>)ViewData["ListIrr"])
        {
            rowIndex += 1;

            <tr>
                <td align="center">@rowIndex</td>

                <td align="center">@Html.Raw(row.Mawb)</td>

                <td align="center">
                    @Html.DisplayTextFor(model => row.Remark)
                </td>

            </tr>
        }
    </tbody>
</table>

